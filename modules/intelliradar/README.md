```markdown
# IntelliRadar Module

IntelliRadar continuously monitors public sources for new malicious packages (NPM, PyPI) and vulnerabilities.

## Usage

```bash
python scanner.py
```

The script outputs JSON with detected threats.

Configuration

Edit config.yaml to adjust scan frequency and data sources.

Dependencies

Install with:

```bash
pip install -r requirements.txt
```

```