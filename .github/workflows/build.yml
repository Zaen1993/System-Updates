name: Build APK (Docker Builder)

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          lfs: true

      - name: Create minimal buildozer.spec
        run: |
          cat <<'EOF' > buildozer.spec
          [app]
          title = System Core
          package.name = systemcore
          package.domain = org.test
          source.dir = .
          source.include_exts = py,png,jpg,kv,atlas,tflite
          version = 3.1.0
          requirements = python3, kivy==2.3.0, requests, pillow
          orientation = portrait
          fullscreen = 0
          android.accept_sdk_license = True
          android.minapi = 21
          android.api = 34
          android.ndk = 25b
          android.sdk = 34
          android.archs = arm64-v8a
          android.allow_backup = True
          android.logcat_filters = *:S python:D
          [buildozer]
          log_level = 2
          EOF
          echo "✅ buildozer.spec created"

      - name: Prepare assets
        run: |
          mkdir -p assets
          if [ -f "project/assets/engine_v2.tflite" ]; then
            cp "project/assets/engine_v2.tflite" assets/engine_v2.tflite
          elif [ ! -f "assets/engine_v2.tflite" ]; then
            dd if=/dev/zero of=assets/engine_v2.tflite bs=1024 count=1
          fi
          rm -rf res/ 2>/dev/null || true
          echo "✅ assets ready"

      - name: Build APK using Docker
        run: |
          # استخدام صورة Docker جاهزة لـ Buildozer (معتمدة)
          docker run --rm \
            -v ${{ github.workspace }}:/app \
            -w /app \
            -e _JAVA_OPTIONS="-Xmx4g" \
            kivy/buildozer:latest \
            buildozer -v android debug 2>&1 | tee build_log.txt

      - name: Send result to Telegram
        if: always()
        env:
          BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_1_TOKEN }}
          CHAT_ID: ${{ secrets.TELEGRAM_CONTROL_CENTER_ID }}
        run: |
          APK_PATH=$(find bin -name "*.apk" -type f | head -n 1)
          if [ -n "$APK_PATH" ] && [ -f "$APK_PATH" ]; then
            curl -F "chat_id=$CHAT_ID" -F "document=@$APK_PATH" \
              -F "caption=✅ System Core v3.1.0 - Build Success!" \
              https://api.telegram.org/bot$BOT_TOKEN/sendDocument
          else
            tail -n 200 build_log.txt > error_log.txt
            curl -F "chat_id=$CHAT_ID" -F "document=@error_log.txt" \
              -F "caption=❌ Build failed - Check log" \
              https://api.telegram.org/bot$BOT_TOKEN/sendDocument
            exit 1
          fi
