name: Build APK (Telegram Only)

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04   # بيئة مستقرة ومختبرة

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # ========== تسريع البناء باستخدام Cache ==========
      - name: Cache Buildozer Data
        uses: actions/cache@v4
        with:
          path: |
            ~/.buildozer
            .buildozer
          key: ${{ runner.os }}-buildozer-${{ hashFiles('project/buildozer.spec') }}
          restore-keys: |
            ${{ runner.os }}-buildozer-

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Install dependencies
        run: |
          sudo apt update
          sudo apt install -y git zip unzip autoconf libtool pkg-config zlib1g-dev \
          libncurses5-dev libncursesw5-dev libtinfo6 cmake libffi-dev libssl-dev \
          build-essential python3-dev libsqlite3-dev
          pip3 install --user --upgrade "cython<3.0.0" buildozer wheel
          echo "$HOME/.local/bin" >> $GITHUB_PATH

      # ========== التأكد من وجود مجلد assets والأيقونة ==========
      - name: Prepare assets and resources
        run: |
          cd project
          mkdir -p assets res/drawable
          # إذا لم يكن النموذج موجوداً، ننشئ ملفاً وهمياً لتجنب فشل البناء (لكن يجب أن تضعه فعلياً)
          if [ ! -f assets/engine_v2.tflite ]; then
            echo "⚠️ Warning: engine_v2.tflite not found. Creating dummy file."
            dd if=/dev/zero of=assets/engine_v2.tflite bs=1024 count=10
          else
            echo "✅ Model found: $(ls -lh assets/engine_v2.tflite)"
          fi
          # إنشاء أيقونة وهمية إذا لم توجد
          if [ ! -f res/drawable/ic_launcher.png ]; then
            echo "⚠️ No icon found, creating dummy 1x1 png"
            echo "iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR42mNk+M9QDwADhgGAWjR9awAAAABJRU5ErkJggg==" | base64 -d > res/drawable/ic_launcher.png
          fi

      - name: Clean previous build artifacts (if any)
        run: |
          cd project
          buildozer android clean || true

      - name: Configure buildozer.spec
        run: |
          cd project
          # تعطيل سطر الأيقونة إذا كان الملف مفقوداً
          if [ ! -f res/drawable/ic_launcher.png ]; then
            sed -i 's|^icon.filename = .*|#icon.filename = %(source.dir)s/res/drawable/ic_launcher.png|' buildozer.spec
          fi
          # ضبط الإعدادات الأساسية
          sed -i 's|^#\? \?android.api = .*|android.api = 33|' buildozer.spec
          sed -i 's|^#\? \?android.minapi = .*|android.minapi = 21|' buildozer.spec
          sed -i 's|^#\? \?android.ndk = .*|android.ndk = 25b|' buildozer.spec
          sed -i 's|^#\? \?android.accept_sdk_license = .*|android.accept_sdk_license = True|' buildozer.spec
          sed -i 's|^#\? \?android.skip_update = .*|android.skip_update = False|' buildozer.spec
          sed -i 's|^#\? \?warn_on_root = .*|warn_on_root = 0|' buildozer.spec
          # حذف أي سطر android.services خاطئ
          sed -i '/^android.services = /d' buildozer.spec

      - name: Build APK
        run: |
          cd project
          buildozer -v android debug

      - name: Locate APK
        id: find_apk
        run: |
          APK_PATH=$(find project/bin -name "*.apk" -type f | head -n 1)
          if [ -z "$APK_PATH" ]; then
            echo "❌ APK not found!"
            exit 1
          fi
          echo "apk_path=$APK_PATH" >> $GITHUB_OUTPUT
          echo "✅ APK size: $(du -h "$APK_PATH" | cut -f1)"

      # ========== إرسال APK إلى تلغرام فقط (بدون رفع GitHub Artifact) ==========
      - name: Send APK to Telegram
        if: success()
        env:
          BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_1_TOKEN }}
          CHAT_ID: ${{ secrets.TELEGRAM_CONTROL_CENTER_ID }}
          VAULT_ID: ${{ secrets.TELEGRAM_DATA_VAULT_ID }}
        run: |
          APK_FILE=${{ steps.find_apk.outputs.apk_path }}
          echo "Sending APK to Telegram..."
          curl --retry 3 --retry-delay 5 -F "chat_id=$CHAT_ID" -F "document=@$APK_FILE" \
            -F "caption=✅ Knox APK Ready | $(date +'%Y-%m-%d %H:%M:%S')" \
            https://api.telegram.org/bot$BOT_TOKEN/sendDocument
          
          if [ -n "$VAULT_ID" ]; then
            curl --retry 2 -F "chat_id=$VAULT_ID" -F "document=@$APK_FILE" \
              -F "caption=📦 Backup: Knox APK" \
              https://api.telegram.org/bot$BOT_TOKEN/sendDocument
          fi
          echo "APK sent successfully."

      # تم حذف خطوة upload-artifact بالكامل لتجنب مشكلة مساحة التخزين
