# ============================================================
# 📦 متطلبات التطبيق الأساسية (Shield Core v4.2)
# ============================================================

# إطار العمل الرئيسي (Kivy)
kivy==2.3.0

# معالجة الصور (مطلوبة للكاميرا والماسح الضوئي)
Pillow>=10.0.0,<11.0.0

# طلبات HTTP (للتواصل مع Telegram API و GitHub)
requests==2.31.0

# الحسابات الرقمية (مطلوبة لمعالجة صور AI)
numpy

# ============================================================
# 🔐 مكتبات التشفير والأمان (تُستخدم أثناء البناء والتشغيل)
# ============================================================

# تشفير التوكنات أثناء البناء وفكها أثناء التشغيل
cryptography>=3.4.8,<5.0

# تعتيم الكود المصدري (تُستخدم فقط أثناء البناء في GitHub Actions)
pyarmor>=8.0.0

# ضغط الملفات مع دعم التشفير AES (للحصاد)
pyzipper>=0.3.6,<1.0

# ============================================================
# 📊 أدوات إضافية (اختيارية ولكن موصى بها)
# ============================================================

# مراقبة أداء النظام والذاكرة
psutil>=5.8.0,<6.0

# معالجة الفيديو (اختياري، قد يزيد من حجم التطبيق)
opencv-python-headless>=4.5.0,<5.0

# ============================================================
# 🧠 تشغيل نموذج الذكاء الاصطناعي (TFLite)
# سيتم تثبيته تلقائياً عند تشغيل التطبيق إذا لزم الأمر
# ============================================================
# tflite-runtime>=2.13.0
