# ============================================================
#  requirements.txt - متطلبات تطبيق Shield Core v4.2
#  تم تحديد إصدارات ثابتة لضمان التوافق والاستقرار.
# ============================================================

# إطار العمل الأساسي
kivy==2.3.0

# معالجة الصور والتحليل
Pillow==10.0.0
numpy==1.24.3

# طلبات HTTP
requests==2.31.0

# تشغيل نموذج TFLite (بديل خفيف عن tensorflow-lite)
tflite-runtime==2.14.0

# تشفير وحماية الملفات (اختياري ولكن موصى به)
cryptography==3.4.8

# إنشاء ملفات ZIP مشفرة (في حال توفرها)
pyzipper==0.3.6

# مراقبة موارد النظام (اختياري)
psutil==5.8.0

# معالجة الصور والفيديو (اختياري، يمكن إزالته لتقليل الحجم)
opencv-python-headless==4.5.5.64

# ============================================================
#  ملاحظات حول الإصدارات:
#  - tflite-runtime 2.14.0 متوافق مع numpy 1.24.3 و Python 3.9+
#  - جميع الإصدارات محددة بدقة لتجنب التعارضات أثناء البناء.
#  - يمكن إزالة opencv-python-headless إذا لم يتم استخدام OpenCV
#    في أي من الوحدات (يُستخدم فقط في camera_analyzer.py كـ fallback).
# ============================================================
